# Build-time only. The published site itself has no runtime dependencies.
#   python -m venv .venv
#   .venv/Scripts/python -m pip install -r requirements.txt   (.venv/bin on macOS/Linux)
Jinja2>=3.1
PyYAML>=6.0
